decorator:
  datasource:
    p6spy:
      enable-logging: true
      multiline: true
      logging: slf4j
      log-file: spy.log
      log-format:
spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/spring_boot_system?serverTimezone=Hongkong
  main:
    allow-bean-definition-overriding: true
